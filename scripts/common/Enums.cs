namespace Editor
{
    public enum ScrollDirection
    {
        FORWARD,
        BACKWARD
    }

    public enum ComponentType
    {
        BULLET,
        BUNDLE,
        SPAWNER,
        TIMER
    }
}
