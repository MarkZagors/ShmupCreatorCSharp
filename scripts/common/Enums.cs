namespace Editor
{
    public static class Enums
    {
        public enum ScrollDirection
        {
            FORWARD,
            BACKWARD
        }

        public enum ComponentType
        {
            BULLET,
            BUNDLE,
            SPAWNER,
            TIMER
        }
    }
}
